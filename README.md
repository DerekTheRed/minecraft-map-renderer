# Minecraft Map Renderer
A tool that reads a Minecraft: Java Edition world or server folder and generates a PNG map of the world.  This tool doesn't extract images from in-game map items, rather, it generates a map image directly from the world's data.
